import { ConversationExceptionInterceptor } from './conversation-exception.interceptor';

describe('ConversationExceptionInterceptor', () => {
  it('should be defined', () => {
    expect(new ConversationExceptionInterceptor()).toBeDefined();
  });
});
